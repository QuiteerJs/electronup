module.exports = {
<%_ if(template === 'vue'|| template === 'vanilla'){ _%>
  extends: ['@quiteer/eslint-config'],
<%_ } _%>
<%_ if(template === 'react'|| template === 'react-swc'){ _%>
  root: true,
  env: { browser: true, es2020: true },
  extends: [
    '@quiteer/eslint-config',
    'plugin:react-hooks/recommended'
  ],
  ignorePatterns: ['dist'],
  plugins: ['react-refresh'],
  rules: {
    'react-refresh/only-export-components': [
      'warn',
      { allowConstantExport: true }
    ]
  }
<%_ } _%>
}
